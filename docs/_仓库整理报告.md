# 仓库目录整理报告 · pale-blue-dot(原 philia-093)

- **整理对象**:`https://github.com/PHILlA093/pale-blue-dot`(原 `philia-093`)
- **操作克隆**:`E:\qg_push\philia-093`(整理只在这个克隆里进行)
- **源码真身**:`E:\workspace\穷观\`(桌面版)、`E:\workspace\穷观手机版\`(手机版)—— **一个字节都没改**
- **分支**:`main`(桌面版)、`mobile-apk`(手机版,含 `手机版/` 与 APK)
- **性质**:仓库树整理,不是代码重构。**未修改任何代码文件内容**。

```
父提交(恢复点)   main        e2b5a0597c9c9329ac24caf273515276c11d3d50
                 mobile-apk  4abd002eaa3ac8f439d8216a57074be251f84805
整理提交          main        64f4a779cd2350e58c5634be809159b801bb6a5c
                 mobile-apk  29bb68afe0b434e06832ded895ccb8a77c778068
```

恢复任何被删/被移的文件:

```powershell
git checkout e2b5a05 -- 英语预热/                       # main 上的例子(目录)
git checkout e2b5a05 -- _gaokao_work/src/ecdict.csv     # main 上的例子(单文件)
git checkout 4abd002 -- 英语预热/                       # mobile-apk 上同理
```

---

## 一、整理前的完整文件清单(按顶层分组,带体积)

数据来源:`git ls-files` 全量 + 逐文件体积(工作区字节数)。

### main(477 个文件,173.49 MB 存储体积 / 174.7 MB 工作区)

| 顶层 | 文件数 | 体积 | 内容概要 |
|---|---:|---:|---|
| `_gaokao_work/` | 196 | 83.30 MB | 知识库重建作业区:`src/`(第三方词表,65.1 MB)、`raw/`、`bench/`、`textbook_src/`、`bio_parts/`、`eng_parts/` + 56 个根级文档/脚本/产物 |
| `桌面版/` | 150 | 75.24 MB | 运行件 6 个(exe、3 个 DLL、使用说明、启动器)+ `数据库/` 3 个 + `build/` **141 个** |
| `英语预热/` | 94 | 8.70 MB | 英语重构前的预热材料(词根版 3500 词、切片 `_work/`、分类 JSON/TXT) |
| `js/` | 11 | 3.78 MB | `app.js`、`train.js`、`demo.js`、`glcanvas.js`、`gltemplates.js`、`mainbridge.js`、`data*.js` × 5 |
| `vendor/` | 3 | 2.61 MB | three.js r128 + OrbitControls + MathJax(本地化) |
| 根文件 | 11 | 0.72 MB | `index/train/guanlan.html`、`favicon.ico`、`README.md`、`LICENSE`、`.gitignore`、`启动穷观学习.bat`、`DESIGN.md`、`_preview.png`、`_preview2.png` |
| `_backup_eng_cloud/` | 5 | 0.22 MB | 旧英语云备份(`app.js.bak` 等) |
| `_promo/` | 6 | 0.07 MB | 桌面版宣传片流水线(脚本 + 说明) |
| `css/` | 1 | 0.04 MB | `style.css` |

### mobile-apk(506 个文件,199.21 MB 存储体积)

= main 的 474 个(少了 `桌面版/build/_指纹.txt`、`_gaokao_work/原创指纹清单.md`、`_gaokao_work/埋指纹报告.md` 三个 **main 独有的后来提交**)
+ `手机版/` 32 个(≈26 MB,含 `穷观学习.apk`)。

其余顶层分组与 main 逐字节一致(`_gaokao_work` 全部 blob SHA 已用 `git cat-file -s` 核对相同)。

---

## 二、四类处置表(总数已配平)

| 处置 | main | mobile-apk | 体积(存储) | 去向 |
|---|---:|---:|---:|---|
| **保留原位** | 61 | 92 | — | 铁律清单全部 + `手机版/` + 构建链路 + `_qa/` |
| **删除(遗留物)** | 213 | 213 | 28.76 MB | git 历史(父提交可取回) |
| **移到 `docs/`** | 54 | 52 | 1.46 MB | `docs/DESIGN.md`、`docs/宣传片/`、`docs/研究资料/` |
| **移出仓库(大体积素材)** | 149 | 149 | 81.85 MB | 仍在 `E:\workspace\穷观\_gaokao_work\` |
| 合计 | **477** | **506** | | |

配平校验:`61+213+54+149 = 477` ✔;`92+213+52+149 = 506` ✔。整理后存储体积
main 173.49 → **62.89 MB**、mobile-apk 199.21 → **88.60 MB**(净减均 110.61 MB)。

### 2.1 删除项的组成(213 个遗留物 + 149 个移出仓库)

| # | 路径 | 文件数 | 存储体积 | 处置 |
|---|---|---:|---:|---|
| A1 | `英语预热/` | 94 | 8.48 MB | 删除 |
| A2 | `_backup_eng_cloud/` | 5 | 0.22 MB | 删除 |
| A3 | `_preview.png`、`_preview2.png` | 2 | 0.31 MB | 删除 |
| A4 | `桌面版/build/_dom{1,3,4,5,6}.html`、`_chemdom.html` | 6 | 0.22 MB | 删除 |
| A5 | `桌面版/build/_*.log` | 24 | 0.09 MB | 删除 |
| A6 | `桌面版/build/_*.png`(调试截图) | 68 | 19.42 MB | 删除 |
| A7 | `桌面版/build/_*.ps1`(一次性探针) | 14 | 0.04 MB | 删除 |
| D1 | `_gaokao_work/src/` | 11 | 65.12 MB | 移出仓库 |
| D2 | `_gaokao_work/bench/` | 14 | 2.52 MB | 移出仓库 |
| D3 | `_gaokao_work/raw/` | 84 | 2.76 MB | 移出仓库 |
| D4 | `_gaokao_work/textbook_src/` | 25 | 2.22 MB | 移出仓库 |
| D5 | `_gaokao_work/*.{json,jsonl,tsv}`(汇总产物) | 14 | 9.28 MB | 移出仓库 |
| D6 | `_gaokao_work/_test.docx`、`_test_in.json` | 1+1 | <1 KB | 移出仓库 |

### 2.2 移到 `docs/` 的 54 项

| 项 | 数量 | 新位置 |
|---|---:|---|
| `DESIGN.md` | 1 | `docs/DESIGN.md` |
| `_promo/` 全部 | 6 | `docs/宣传片/` |
| `_gaokao_work/*.md`(9) + `人教版高中英语_单元词表.json`(1) | 10 | `docs/研究资料/` |
| `_gaokao_work/*.{js,py,ps1}` 工具脚本 | 23 | `docs/研究资料/` |
| `_gaokao_work/*_report.txt`、`*_stats.txt`、`near_dups.txt` | 8 | `docs/研究资料/` |
| `_gaokao_work/bio_parts/`(5)、`eng_parts/`(1) | 6 | `docs/研究资料/{bio_parts,eng_parts}/` |

**54 项里有 51 项是 100% 纯改名**(blob 逐字节一致,见 §五);仅 3 项带内容改动
(`docs/宣传片/README.md` 88%、`docs/研究资料/原创指纹清单.md` 96%、`埋指纹报告.md` 95%),改动内容见 §四。

---

## 三、我确认删掉了什么,依据是什么

每条都先**打开看内容** + **全仓 grep 引用**,再处置。

### A1 `英语预热/`(94 文件)—— 英语重构前的遗留物 ✂

判断依据(三条独立证据,任一成立即"已被取代"):

1. **内容自证**:目录内 `_work/生成_英语知识云DB.ps1` 第 1 行写着
   `Generate js\data_eng.js : ENG_DB knowledge cloud from the English-prep knowledge base.`
   —— 它就是**旧** `js/data_eng.js` 的生成器;`README_英语预热知识库.md` 自述"本知识库只在本目录内新增文件"。
2. **现状对照**:`js/data_eng.js` 头部注释现为"人教版 2019 课标版重构",实测
   `boards=10 / points=3053 / links=2756`;而旧库是 6 板块 / 1978 点(见 `知识点现状清单.md`)。
   重构后新库只从旧库保留了词根词缀/核心句型/语法知识三类工具内容,预热目录整体失效。
3. **引用检查**(`git grep -I -F` 全仓):
   - 命中 39 处,其中 **36 处在 `英语预热/` 目录内部**(自引用);
   - `_gaokao_work/{count,canonical,parse_candidates}.js` 各 1 处,形式是
     `'E:/workspace/穷观/英语预热/高考英语3500词根版_全量.json'` —— 指向**源码真身**的绝对路径,
     与仓库内本目录无关(删除本目录不影响这些脚本);
   - `桌面版/数据库/qg_subjects.txt:2404` 与同名 `.bak:2197` 各 1 处,是知识云内嵌的
     **历史文案**「说明: 本知识云由「英语预热知识库」自动组装」,属于数据内容、不是路径依赖,
     且 `桌面版/数据库/` 属铁律不动 → 未改(见 §七 第 4 条)。
   - README、任何 `*.bat`、`*.ps1`、html、js **零引用**。
4. 该目录**不是**构建输入:`_rebuild.ps1` 的资源清单里没有它。

### A2 `_backup_eng_cloud/`(5 文件)✂

内容为 `app.js.bak`、`app.js.perf_v2.bak`、`glcanvas.js.pre_segfix.bak`、`index.html.bak`、
`_rebuild.ps1.bak` —— 同一时期工作文件的 `.bak`,文件名即"旧版本"语义。
全仓 grep:`_backup_eng_cloud` 仅 1 处命中,在 `原创指纹清单.md` 的**排除目录正则**里,
不是依赖。当前 `js/app.js`、`js/glcanvas.js`、`index.html`、`桌面版/build/_rebuild.ps1` 均存在且更新。

### A3 `_preview.png`、`_preview2.png` ✂

全仓 grep `_preview`:除这两个文件自身外**零命中**(README 未引用 → 无需先修 README)。

### A4–A7 `桌面版/build/` 调试残留(112 文件,19.76 MB)✂

打开抽样确认:

- `_dom1.html` 标题为「穷观 **V1.0** · 高中数学知识网络」、`_dom4.html`/`_chemdom.html` 为「**V1.2.0**」,
  而当前版本是 **V2.4.2** —— 是抓取页面 DOM 的旧快照。
- `_cb1.log` 等是 Edge 无头运行的 stderr(内含 `msedge.exe : [14788:10432:0904/...]` 与 PowerShell 报错原文)。
- `_slowtest.ps1`/`_frameless.ps1`/`_scan1-3.ps1`/`_intro_*.ps1`/`_vercheck.ps1`/`_swtest.ps1`/
  `_train_e2e.ps1`/`_wipe_e2e.ps1` 全部是 `Add-Type user32.dll + PrintWindow + GetPixel` 式
  "截屏/数像素"一次性探针,读取的正是上面那些 `_*.png`;功能已被 `_qa/` 的 CDP 探针套件取代。
- `_scan1/2/3.ps1` 与 `_n_m0.png`/`_n_i1.png`/`_sw1.png` 互相引用 —— 脚本与图一起删,不留悬空引用。

**构建链路一个都没删**(逐条 grep `_rebuild.ps1` 得到的引用清单):

| `_rebuild.ps1` 引用 | 处置 |
|---|---|
| `_makeicon.ps1`(第 48 行 `& (Join-Path $build '_makeicon.ps1')`) | **保留** |
| `_web_icon.ico`(第 47/51/53 行) | 由 `_makeicon.ps1` 现场生成,本就被 `.gitignore` 忽略、不在树里(无可删) |
| `Program.cs`(第 65 行 `$src`) | **保留** |
| `_指纹.txt`(第 61–64 行,`/resource:…,web.qg.fingerprint.txt`) | **保留** |
| `Microsoft.Web.WebView2.Core.dll`、`…WinForms.dll`(第 19–20 行 `/r:`) | **保留** |
| `_app_tmp.exe`(第 14 行 `/out:` 临时输出) | 本就 `.gitignore` 忽略、不在树里 |
| `_qa/`(22 个回归探针,含 `_qa/server.js`) | **保留**(用户明确要求留作回归测试) |

结果:`桌面版/build/` 从 141 → **29**(main)/ **28**(mobile-apk,无 `_指纹.txt`),
留下的正好是 `Program.cs`、`_rebuild.ps1`、`_makeicon.ps1`、`_指纹.txt`、3 个 DLL、`_qa/` 22 个。

### D1–D6 移出仓库的 149 个文件(81.85 MB)✂

不是"删除",是**从仓库取出、原件留在本机**。分类依据:

- **D1 `src/`(65.12 MB)**:三方词表/词典原始素材(`ecdict.csv` ≈65 MB、`pluto_3500.txt`、
  `Lazuli_raw.txt`、`citu_*`、`ckaorceu_words.json`、`wordforest_words_all.json`、
  `Gumingyu_index.html`)。**非本项目原创**,体积占全仓 1/3 以上,公开仓再分发有版权顾虑。
- **D2 `bench/`、D3 `raw/`**:第三方基准与题库(`gaokao_bench` CSV、`agieval_*.jsonl`、
  `Gaokao-EN-Eval.xlsx`、`gkb_*.json`、`gaokao_en/gaokao1..75`)。构建输入,非成果。
- **D4 `textbook_src/`**:抓取/转换用教材源(11 个 `PEPGaoZhong_*.zip`、`pep2019-*.json`)。构建输入。
- **D5 汇总产物(9.28 MB)**:`union*.{json,tsv}`、`canon_freq.*`、`freq.*`、`canonical.json`、
  `corpus*.jsonl`、`doc_data.json`、`textbook_words_B.jsonl`、`gap2.json`。可由**保留下来的脚本**
  + 上述原始输入重新生成,且内容已被成品 `js/data*.js` 覆盖。
- **D6 `_test.docx`/`_test_in.json`**:排版测试临时件。

**两处实测重复文件**(SHA256 完全相同,一并移出):

```
IDENTICAL  _gaokao_work/raw/agieval_v1_1_gaokao-english.jsonl
           _gaokao_work/raw/agieval_v1_gaokao-english.jsonl            (660,925 B)
IDENTICAL  _gaokao_work/src/Gumingyu_index.html
           _gaokao_work/textbook_src/rjb-word-review_index.html         (128,557 B)
```

另外两组只是**体积巧合**(2205 B / 1937 B 各两对),SHA256 不同,不是重复,未按重复处理。

原件位置与逐项说明写进了 **`docs/研究资料/README.md` 第三节**(不假装它们从没存在过)。

---

## 四、引用同步修复(全部落地,零悬空)

| 文件 | 改了什么 | 依据 |
|---|---|---|
| `README.md` | `架构细节见 DESIGN.md` → `docs/DESIGN.md`;源码结构块补 `docs/DESIGN.md`/`docs/研究资料/`/`docs/宣传片/` 三行与 `桌面版/build/` 的内容说明;新增仓库地址 `https://github.com/PHILlA093/pale-blue-dot` 与 `main`/`mobile-apk` 两分支说明 | 全仓 grep `DESIGN.md` 仅 README:112 一处引用;`pale-blue-dot` 原本 **0 命中**(README 此前根本没写仓库地址) |
| `.gitignore` | 新增 `*.tmp`、`*.log`、`*.bak_*`(已有)、`_*.png`、`_dom*.html`、`_shots*`;原 `_st_*.png`、`_console*.log` 被 `_*.png`/`*.log` 覆盖故删除;`_prof*/`、`frames*/`、`__pycache__/`、`*.pyc`、`webview2.*`、`_app_tmp.exe`、`_web_icon.ico`、`*.exe.WebView2/` 原有保留 | 用户点名的 `_prof*/`、`*.bak_*`、`_console*.log`、`frames*/` **原本就有**;缺的是 `_shots*/`、以及本次删掉的那几类残留的兜底模式 |
| `docs/宣传片/README.md` | 示例命令里的 `_promo\record.js`/`music.py`/`assemble.ps1` 等 7 处 → `docs\宣传片\…`;**指向仓库外部的 `E:\qg_promo\…` 路径按原样保留** | `qg_promo` 是外部工作目录,不是仓库内路径,不能一起替换 |
| `docs/研究资料/原创指纹清单.md`、`埋指纹报告.md` | 顶部/回退点后补「整理注记」说明新位置;报告里 `remote git@github.com:PHILlA093/philia-093.git` → 现为 `pale-blue-dot.git`(并保留"撰写当时为 philia-093"的事实) | 全仓 grep `philia-093` 仅此 1 处;历史事实不抹掉、旧名不残留 |
| `docs/研究资料/README.md`(新增) | 说明本目录内容、脚本的绝对路径约定、移出仓库的清单与原位置、数字口径差异 | 新增文件,无引用问题 |

grep 复核结果(整理后工作区,排除 `.git`):

```
英语预热 / _backup_eng_cloud / _preview            → 仅 1 处历史文案 + 已注明的历史命令记录
DESIGN.md                                          → 只剩 docs/DESIGN.md 形式
philia-093                                         → 仅"撰写当时"的历史陈述
_promo\ 自引用                                      → 0(外部 E:\qg_promo\ 保留 5 处,属正常)
```

**空目录**:移动后本地留下的空壳目录已删除,共 **20 个**(含 `英语预热/` 及其 5 个子目录、
`_gaokao_work/` 树、`_promo/`、`_promo/__pycache__`、`_backup_eng_cloud/`)。

---

## 五、核验结果(推送后,针对 `origin/main` 与 `origin/mobile-apk`)

### ① 铁律清单逐项在位 —— 全部 OK

两个分支逐项检查,以下路径**位置未变、均存在**:`index.html`、`train.html`、`guanlan.html`、
`favicon.ico`、`css/`、`js/`、`vendor/`、`启动穷观学习.bat`、`README.md`、`LICENSE`、`.gitignore`、
`桌面版/`(含 `穷观学习.exe`、`使用说明.txt`、`启动穷观学习.bat`、3 个 WebView2 DLL、`数据库/`、
`build/Program.cs`、`build/_rebuild.ps1`、`build/_makeicon.ps1`、`build/_指纹.txt`(main)、`build/_qa/`)、
`手机版/`(mobile-apk,含 `穷观学习.apk`)。**0 项缺失。**

### ② 目录文件数:整理前基线 → 推送后

| 目录 | 基线(main) | 现在(main) | 结论 |
|---|---:|---:|---|
| `css/` | 1 | 1 | **完全一致** ✔ |
| `js/` | 11 | 11 | **完全一致** ✔ |
| `vendor/` | 3 | 3 | **完全一致** ✔ |
| `桌面版/build/_qa/` | 22 | 22 | **完全一致** ✔ |
| `桌面版/数据库/` | 3 | 3 | **完全一致** ✔ |
| `桌面版/` | 150 | 38 | −112 |

| 目录 | 基线(mobile) | 现在(mobile) | 结论 |
|---|---:|---:|---|
| `css/` / `js/` / `vendor/` | 1 / 11 / 3 | 1 / 11 / 3 | **完全一致** ✔ |
| `桌面版/build/_qa/` / `桌面版/数据库/` | 22 / 3 | 22 / 3 | **完全一致** ✔ |
| `手机版/` | 32 | 32 | **完全一致** ✔ |
| `桌面版/` | 149 | 37 | −112 |

> **⚠ 关于 `桌面版/` 计数的一处口径冲突,如实说明:**
> 最初的任务书写"确认 `桌面版/` 的文件数与整理前完全一致";随后的补充要求又明确指示删除
> `桌面版/build/` 下的调试产物(`_dom*.html`、`_app_tmp.exe`、`_web_icon.ico`、`_prof*`、`*.log`、
> `*.png`、临时脚本)。两者不可能同时成立,按**后一条**执行,因此 `桌面版/` 总数 −112。
> 可以确定的是:**减掉的 112 个全部在 `桌面版/build/` 内,且全部是调试产物**;
> `桌面版/` 下除 `build/` 以外的 9 个路径(运行件 6 + `数据库/` 3)**计数与内容都完全没变**。
> 更硬的证据见 ③:所有功能文件的 **blob SHA 与整理前逐字节一致**。

### ③ 内容级核验(比计数更强):必须是"一个字节都没变"的部分

以整理前提交的 `git ls-tree -r` 输出为基线,逐条比对推送后的 blob SHA:

```
main        基线 e2b5a05 共 61 项:缺失 0,内容变更 0   (README.md / .gitignore 为有意编辑,已排除)
mobile-apk  基线 4abd002 共 92 项:缺失 0,内容变更 0   (同上)
```

即:`index.html`、`train.html`、`guanlan.html`、`favicon.ico`、`css/`、`js/`、`vendor/`、
`桌面版/` 的宿主与构建件、`启动穷观学习.bat`、`LICENSE`,以及 mobile 的 **`手机版/` 全部 32 项**,
**内容与整理前完全相同**。

改名部分:54 项中 **51 项为 100% 纯改名**(blob 完全一致),3 项为有意编辑(88%/95%/96%)。

### ④ 被删对象确认不在树里

两个分支皆:`英语预热/`、`_backup_eng_cloud/`、`_gaokao_work/`、`_promo/`、`_preview.png`、
`_preview2.png` 命中 **0**;`桌面版/build/_dom*.html`、`*.log`、`_*.png`、直系 `_*.ps1`(排除
`_rebuild.ps1`/`_makeicon.ps1`)命中 **0**。

### ⑤ 推送

```
$ git push origin main          →  e2b5a05..64f4a77  main -> main
$ git push origin mobile-apk    →  4abd002..29bb68a  mobile-apk -> mobile-apk
（均为 fast-forward,未使用 --force,未改写历史）

$ git ls-remote --heads origin
64f4a779cd2350e58c5634be809159b801bb6a5c        refs/heads/main
29bb68afe0b434e06832ded895ccb8a77c778068        refs/heads/mobile-apk
```

### ⑥ 整理后的树

| 分支 | 顶层 | 文件数 |
|---|---|---:|
| main | `css/ js/ vendor/ 桌面版/ docs/` + 8 个根文件 | 116 |
| mobile-apk | 同上 + `手机版/` | 145 |

`docs/` 结构:`DESIGN.md`(1)、`宣传片/`(6)、`研究资料/`(main 48 / mobile 46)。

---

## 六、两个分支的做法

未用 cherry-pick(两分支的 `js/`、`html`、`css` 内容并不完全相同,
`_gaokao_work` 也差 2 个文件,cherry-pick 有冲突风险),而是**用同一个脚本各跑一遍**:
`E:\qg_push\_cleanup.ps1` 按正则规则读 `git ls-files`,先在 main 上 dry-run 核对,
再在两个分支分别执行、分别提交。这样每个分支的改动都精确匹配它自己的文件集:

- main:删 362 + 移 54 = 416 项;mobile-apk:删 362 + 移 52 = 414 项(少 2 个 main 独有的指纹文档)。
- 4 个纯文本修复(`README.md`、`.gitignore`、`docs/宣传片/README.md`、`docs/研究资料/README.md`)
  在两分支上**逐字节相同**(整理前这 4 个文件的基线本就一致,直接用 `git checkout main -- <path>`
  取同一份)。
- **`手机版/` 在 mobile-apk 上一个字节未动**:`git diff --cached --name-only -- 手机版` 输出为空。

---

## 七、我不确定 / 没敢动 的清单

1. **`桌面版/数据库/qg_subjects.txt.bak`(1.47 MB,已入库)→ 保留未动。**
   它由 `Program.cs` 的原子写产生(先写 `.tmp` 再 `File.Replace`,留一份 `.bak`),
   `DESIGN.md` §3 明确记载了这个机制。删掉它等于撤掉用户本机知识云的一道保险,
   而且它能被程序自动重建(所以留着只是"不整洁",不是"错"),无法确定是否有意入库 → 交给用户定夺。
   全仓 grep `.bak` 零引用,确认无人依赖。**建议**:确认不需要后 `git rm` 该文件。

2. **`桌面版/build/_指纹.txt` 里的一句旧路径指针 → 未改。**
   第 16 行写「逐条对照与举证命令见《原创指纹清单.md》(**穷观/_gaokao_work/**)」。
   不改的理由:(a) 该文件是 `_rebuild.ps1` 以 `/resource:` **内嵌进 exe 的构建资源**,
   改动会让磁盘副本与已发布的 `穷观学习.exe` 不一致;(b) 审计文档 `原创指纹清单.md` 明确记录了
   "本文件全文出现在 exe 偏移 **6732740**",改动会破坏这条字节偏移证据;
   (c) 该指针写的是 `穷观/_gaokao_work/`,指的是**源码真身** `E:\workspace\穷观\_gaokao_work\`,
   那里 `原创指纹清单.md` **确实还在** —— 严格说它并**不是**悬空引用,只是仓库里也放过一份。

3. **`README.md`「科目与规模」表的数字 → 未改(本次最值得复核的一处)。**
   用户约束是"只允许改 README 里的**路径/名字**引用",数字属于内容,故未越界修改。
   README 现写(并自述"数字取自当前 `js/data*.js`"):

   | 科目 | README 现写 | 实测(解析 `js/data*.js`) |
   |---|---|---|
   | 数学 | 10 板块 / 115 / 263 | 10 / **132** / **656** |
   | 化学 | 8 / 86 / 219 | 8 / 86 / **438** |
   | 物理 | — / 92 / — | 10 / 92 / 304 |
   | 英语 | — / **1978** / — | 10 / **3053** / 2756 |
   | 生物 | *(表里没有这一行)* | 5 / 137 / 404 |

   其中 **英语 1978 正是"英语重构前"的旧值**(旧库 6 板块 / 1978 点,见 `知识点现状清单.md`),
   与 `英语预热/` 属同一批遗留 —— 若要"清透",这一格也该改成 3053,并且表里缺 `js/data_bio.js` 对应的生物。
   两分支的 `js/data*.js` 只差一行指纹注释,所以这些数字对两个分支同样适用。
   **建议**:把该表改为实测值并补生物一行 —— 一行命令即可复核:
   `node -e "…"` 或参见本报告 §五 的口径。

4. **`桌面版/数据库/qg_subjects.txt`(及 `.bak`)内嵌的历史文案**
   「说明: 本知识云由「英语预热知识库」自动组装, AI整理初版, 待人工校对。」→ 未改。
   这是**数据文件内容**,且 `桌面版/数据库/` 属铁律路径;改它等于改用户的知识云数据。
   若要清掉这句话,应通过 App 重新上传知识云来生成,而不是直接编辑数据库文件。

5. **`docs/研究资料/知识点现状清单.md`、`资料源_高中教材与笔记.md` 里的英语旧口径**(1978 点 / 6 板块)
   → 正文未改(按"历史记录不改写"),已在 `docs/研究资料/README.md` 第四节写明时间差与当前值。

6. **两份指纹文档里的历史命令**中出现的 `_promo`、`_promo_phone`、`_backup_eng_cloud`、`_gaokao_work`
   排除目录项(如 `robocopy … /XD _shots _gaokao_work _promo_phone`、
   `-notmatch '\\(_shots|_promo|_backup_eng_cloud|…)\\')`)→ 按原样保留。
   它们是可执行命令的逐字记录,整理后这些目录已不在仓库,对应条目变成"永不命中"的空操作,
   **不影响命令继续执行**,故不改写;已在 `docs/研究资料/README.md` 第二节说明。

7. **`_promo_phone`(手机版宣传片)** —— 被两份指纹文档引用,但**整理前两个分支的树里都没有这个目录**
   (mobile-apk 只有 `手机版/`,没有 `_promo_phone/`)。本次无对象可动,仅记录;
   若它存在于本机某处,属未入库内容。

8. **`_gaokao_work/_test.docx` / `_test_in.json`** —— 判为排版测试临时件并移出仓库(体积极小)。
   若它们其实是刻意的排版验收样本,可从父提交取回。

9. **`_backup_eng_cloud/` 里的 `.bak` 内容** —— 已按指示删除。仅记录:
   其中 `index.html.bak`、`app.js.bak`、`glcanvas.js.pre_segfix.bak` 是**英语重构前**的版本,
   若日后想比对旧实现,需 `git checkout e2b5a05 -- _backup_eng_cloud/`。

10. **未做的事**(约束所限,非疏漏):未改动任何代码文件内容、未移动铁律清单路径、
    未 `--force`、未 `filter-branch`/`rebase -i`,未启动 exe / APK / 模拟器 / 浏览器,未弹出任何窗口。

---

## 八、可复现的操作记录

```powershell
# 1) 清点与判断
git ls-files                                  # 477(main) / 506(mobile-apk)
git grep -n -I -F -- '英语预热'                 # 39 命中,36 在目录内,3 在指向源码真身的绝对路径
git grep -n -I -F -- '_preview'                # 0 命中
git grep -n -I -F -- '_rebuild.ps1'            # 构建链路引用清单
git cat-file -s e2b5a05:_gaokao_work/src/ecdict.csv   # 65162816(与 mobile 同名文件一致)

# 2) 整理(dry-run 先看计划,再执行)
powershell -NoProfile -ExecutionPolicy Bypass -File E:\qg_push\_cleanup.ps1 -DryRun
powershell -NoProfile -ExecutionPolicy Bypass -File E:\qg_push\_cleanup.ps1

# 3) 提交与推送(两个分支各一次,均 fast-forward)
git add -A; git commit -F <msg>
git push origin main; git push origin mobile-apk

# 4) 核验
git ls-tree -r --name-only origin/main
git rev-parse origin/main:<铁律路径>            # 与 e2b5a05:<同一路径> 比 blob SHA
git ls-remote --heads origin
```
