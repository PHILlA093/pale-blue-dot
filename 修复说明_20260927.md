本次修复已完成，日期：2026-09-27。范围为此前审查发现的内容错误、数据保存、AI 出题校验和安装卸载问题。没有操作真实用户的笔记、API Key 或 WebView2 个人配置，也没有执行真实安装、卸载。

已经修复：

- 观澜重复窗口：入口直接打开独立窗口，主窗已移除观澜面板、“拆为独立窗口”按钮及观澜画布脚本。桌面宿主打开原生窗口后返回 null 时，不再误判为弹窗拦截。打开失败只提示错误。

- 知识内容：纠正数学“存在递增区间”的错误判据；修正英语 aster/astr/astro 词根的错误归类、television 与 visit 的音标，以及 tutor 释义中混入的内容。其它带“待人工校对”的条目在详情顶部明确提示，不能视为全库已经校对。
- 笔记保存：兼容旧笔记，新增笔记按记录独立保存，避免多个窗口新增时互相覆盖；空间不足或存储失败时提示错误并保留输入。
- 出题稳定性：固定请求开始时的科目和知识点，严格检查四题的题型、难度字段、选项、答案、解析与重复题。只有新题通过检查才替换旧题；失败保留旧题和已展开解析。格式错误最多重试三次，连接失败和输出截断不自动反复请求。
- 来源显示：不再仅凭 AI 写出的“某年真题”认定来源。题干、选项必须按顺序匹配本次检索片段，才显示“本地原文匹配”或“联网原文匹配”；不能匹配的来源显示待核实。原文匹配不代表答案正确，也不证明网页具有真题身份。
- 高考英语训练：英语使用阅读理解（单选）、语法填空、书面表达入口，要求英文题目、中文解析；难度与素材偏好分开选择。
- 桌面通信与模型请求：修复资料库回执没有被接收造成的等待/重复上传；保留不同消息监听器需要的请求编号；兼容旧模型别名，补齐 JSON 输出和思考模式参数，识别输出被截断的情况。
- 卸载保护：WebView2 目录保存着真正的笔记、自绘函数和设置，卸载时保留该目录及个人资料索引备份。

验证结果：

- `node --test tests/regression.cjs tests/guanlan-window.cjs`：18 项通过，0 项失败；包含五科数据结构完整性、并发新增笔记、保存失败、出题结构/来源校验、失败保留旧题、模型参数及桌面通信回执。
- `tests/InstallerRegression.cs`：通过。仅在临时目录测试清理逻辑，确认学习配置、个人索引及备份、无关文件均被保留。
- 浏览器隔离测试：数学页面正常加载，新增笔记刷新后可找回，英语入口正确切换；四题及数学公式正常显示，模拟连接失败/无效响应保留旧题和已展开解析。使用假密钥与模拟响应，没有调用真实 AI。验证截图见 `tests/修复验证_20260927.png`。
- 观澜专项回归：模拟桌面窗口已打开但返回 null、网页弹窗被拦截或抛错、重复绑定与重复点击，确认主窗不会显示观澜面板。修复前 3 项失败，修复后通过。浏览器另行核对主窗没有面板，独立页聊天与画布正常加载、无控制台错误。
- 桌面快捷方式 `D:\OneDrive\Desktop\穷观学习.lnk` 指向 `E:\workspace\穷观\桌面版\穷观学习.exe`，目标哈希与本次构建一致。快捷方式不需要修改。
- 最终桌面程序编译成功，20 个内嵌资源的 SHA256 与当前文件完全一致。
- 安装程序和卸载程序编译成功；安装包自检通过；6 个安装载荷及内嵌卸载程序解压后的 SHA256 与最终文件一致。
- 下载 ZIP 的 CRC 检查通过，内含安装程序与最终安装程序逐字节相同，原安装说明保留不变。

可使用的文件：

- `桌面版/穷观学习.exe`：已更新。请从原目录启动，保持同目录的三个 WebView2 DLL 和个人配置目录在原位。
- `桌面版/dist/穷观学习_安装程序.exe`：已更新。
- `下载/穷观学习_桌面版_v2.4.2_安装包.zip`：已更新，保留原有版本号。

最终文件 SHA256：

```text
穷观学习.exe
1728208FD2EFBC5FA41F4DF8727D36CD27EC43B7466376EC77B9739318F1DCFB

穷观学习_安装程序.exe
0E898DE066703A6FD874238B7D4E29078C79D3E300F478C37EF1F4EAEE666245

穷观学习_桌面版_v2.4.2_安装包.zip
B0D8F43F03BC556D4968636DC6EA24048C44C4834142153E179F883BF2ECFD22
```

回退备份位于 `_backup_fixes_20260926_201007/`，17 个原文件与 `manifest.json` 的原始校验值一致。需要回退时，先退出程序，再按原相对路径复制需要恢复的程序/源码文件；不要删除或覆盖 `.WebView2` 个人配置目录。新版新增笔记使用独立的 v2 记录，旧版程序不会显示这些新记录，但回退不会删除它们，恢复修复版后可以继续读取。

尚未验证的范围：真实 DeepSeek 服务的付费请求、全部知识内容与生成答案的学科正确性，以及不同电脑上的完整安装运行流程。本次未新增错题本、作答评分或学习计划等较大功能。

观澜专项修复前的备份：`_backup_guanlan_20260927_012711/`。本次只修改主窗口的观澜结构、打开入口和相关说明/测试，并重建相关程序与安装包；独立观澜页的 AI、画布和个人数据逻辑未修改。截图：`tests/观澜独立窗口验证_20260927.png`。
