# --- runtime data: holds the local DeepSeek API key in plain text, never commit it ---
*.exe.WebView2/
# --- build / debug artifacts ---
_app_tmp.exe
_web_icon.ico
_prof*/
pkg/
webview2.zip
webview2.nupkg
*.tmp
*.log
*.bak_*
# --- one-off UI probes: screenshots and captured DOM snapshots, dev-time only ---
_*.png
_dom*.html
_shots*/
__pycache__/
*.pyc
# --- promo render cache / local scratch ---
frames*/
# --- installer build outputs: regenerated by 桌面版/build/_build_installer.ps1 ---
#     (the shipped zip itself lives in 下载/ and IS committed)
qg.payload.*.gz
qg.uninstaller.gz
_inst_*.ico
_inst_*.png
_inst_tmp.exe
_uni_tmp.exe
dist/
卸载穷观学习.exe
